project:
  name: "VLANeXt_libero_object_latent_bridge_resnet18_b16_30k"
  output_dir: "/home/dm/QWENLA/VLANeXt_migration/VLANeXt/checkpoints"
  log_interval: 20
  profile_timing: true
  timing_interval: 20
  timing_sync_cuda: true
  save_interval: 5000
  use_wandb: true

data:
  dataset_name: "libero"
  data_root: "/home/dm/QWENLA/VLANeXt_migration/data/LIBERO_modified"
  task_suite_name: "libero_object_no_noops"
  full_sequence: true
  latent_bridge_sequence_len: 32
  history_len: 8
  future_len: 8
  buffer_size: 32
  batch_size: 8
  max_steps: 30000
  num_workers: 2
  input_modality: "image"
  view_mode: "multi"
  augmentation:
    enabled: true
    random_resized_crop:
      scale: [0.8, 1.0]
      ratio: [0.9, 1.1]
    random_brightness: [0.2]
    random_contrast: [0.8, 1.2]
    random_saturation: [0.8, 1.2]
    random_hue: [0.05]
    augment_order:
      - "random_resized_crop"
      - "random_brightness"
      - "random_contrast"
      - "random_saturation"
      - "random_hue"

model:
  model_type: "vlanext"
  lmm_path: "/home/dm/QWENLA/VLANeXt_migration/VLANeXt/pretrained/Qwen3.5-0.8B"
  vision_encoder_path: "google/siglip2-base-patch16-256"
  backbone_mode: "frozen"
  attn_implementation: "sdpa"
  action_dim: 7
  loss_type: "diffusion"
  num_train_timesteps: 1000
  num_inference_timesteps: 10
  scheduler_type: "flow_match"
  num_bins: 256
  action_vqvae:
    enabled: false
    frozen: true
    codebook_size: 1024
    hidden_size: 256
    depth: 4
    num_heads: 8
    steps: 1000
    learning_rate: 1.0e-3
  condition_type: "soft"
  num_queries: 16
  use_transformer_connector: true
  connector_depth: 2
  connector_num_heads: 4
  policy_hidden_size: 1024
  policy_depth: 24
  policy_num_heads: 16
  policy_mlp_ratio: 4.0
  future_image_loss_weight: 0.0
  generator_hidden_size: 768
  generator_depth: 29
  generator_num_heads: 12
  generator_mlp_ratio: 4.0
  future_image_mode: "horizon"
  dct_loss_weight: 0.0
  use_progress_head: false
  use_progress_film: false
  use_vlm_layer_attention: false
  vlm_layer_indices: null
  use_latent_bridge: true
  latent_bridge_num_heads: 8
  latent_bridge_max_views: 4
  latent_bridge_image_size: 224
  latent_bridge_refresh_interval: 32
  latent_bridge_visual_trainable: true
  latent_bridge_vlm_uses_proprio: false
  progress_loss_weight: 0.0
  progress_detach: true
  progress_noise: 0.0
  log_loss_components: true
  dct_freq_split: 0.125
  dct_low_freq_weight: 5.0
  dct_high_freq_weight: 0.0
  dct_similarity_type: "mse"
  use_proprio_input_vlm: true
  use_transformer_proprio_projector: false
  projector_depth: 2
  projector_num_heads: 4
  use_action_input_policy: false
  gradient_checkpointing: true

train:
  seed: 2026
  resume_path: ""
  pretrained_checkpoint: ""
  learning_rate: 1.0e-4
  weight_decay: 0.01
  warmup_steps: 500
  gradient_accumulation_steps: 2
  max_grad_norm: 1.0
  device: "cuda"
  distributed: false
  dist_backend: "nccl"
