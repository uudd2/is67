# VLANeXt 最新 Baseline 技术报告

## 1. Baseline 身份

本文将当前 GPU1 上运行的实验定义为最新 baseline：

```text
actioneffect_v3_compgate04_globalres01ffn4_globalrelqk128
finesrc_mainview_q4_cross0_global24_nodct_bs16_micro16_libero_long
```

对应资源：

```text
训练配置：
config/libero_long_train_vita_hiermq54_actioneffect_v3_compgate04_globalres01ffn4_globalrelqk128_finesrc_mainview_q4_cross0_global24_bs16_micro16_config.yaml

训练日志：
train_compgate04_globalres_globalrelqk128_micro16_gpu1.log

WandB：
project = VLANeXt_vita_hiermq54
run_id = nbjyps7u
https://wandb.ai/2979416990-northwestern-university/VLANeXt_vita_hiermq54/runs/nbjyps7u
```

2026-07-30 21:13 快照：

```text
GPU：1
PID：137406
进度：约 24,660 / 160,000
当前速度：约 1.86 step/s
当前总 loss：约 0.02～0.03
首个 checkpoint：32,000 step
```

GPU0 上的 `visualoff_resume128k` 是关闭视觉辅助头的后期消融，不属于本 baseline。

## 2. 总体结构

```text
双视角图像 + 语言指令
        ↓
Qwen3.5-0.8B，仅执行前 12 个语言层，参与微调
        ↓
hidden states: H0, H1, ..., H12
        ↓
层级 MQ：28 local + 24 global
机器人状态：额外生成 2 state tokens
        ↓
最终条件 C: [B, 54, 1024]
        ├─────────────────────────────┐
        ↓                             ↓
Gated weighted pooling          8-layer latent Flow
        ↓                             ↑
z_obs: [B, 1024]                     │
                                      │
动作 chunk [B,8,7] → ActionEncoder → z_act [B,1024]
                                      │
              z_tau=(1-tau)z_obs+tau z_act
                                      ↓
                       Flow 预测 dz/dtau
                                      ↓
                    6-step Euler rollout
                                      ↓
                       predicted z_act
                                      ↓
                      ActionDecoder
                                      ↓
                       action [B,8,7]
```

另有一个仅训练期使用的 Q4 视觉动作效果辅助分支。

## 3. 输入与 VLM

### 3.1 数据

```text
数据集：LIBERO libero_10_no_noops
输入：multi-view 图像 + 语言指令 + robot state
动作 chunk：8 × 7
history_len：8
future_len：8
```

策略 VLM 使用多视角输入；视觉动作效果辅助分支只使用主视角。

### 3.2 VLM

```text
模型：pretrained/Qwen3.5-0.8B
语言 hidden dim：1024
原始语言层数：24
实际执行层数：前 12 层
语言 attention heads：8
视觉编码器：12 层，hidden dim 768
VLM 状态：微调
attention implementation：SDPA
```

VLM 输出所有已执行层的 hidden states，供不同来源的 MQ 查询。

## 4. MQ54 的产生

### 4.1 Local MQ：28 个

```text
来源          token 范围        MQ 数量       查询范围
H0 visual     local 0:8         8             只读视觉 token
H0 text       local 8:12        4             只读文本 token
H1            local 12:16       4             读取该层全部有效 token
H4            local 16:20       4             读取该层全部有效 token
H8            local 20:24       4             读取该层全部有效 token
H12           local 24:28       4             读取该层全部有效 token
```

每组使用独立 learned queries，通过 8-head cross-attention 从对应 hidden state 提取信息，再投影到 1024 维。

本版本没有动态 TopK、router 或随机 MQ 选择，28 个 local MQ 全部保留。

### 4.2 Cross MQ：0 个

```text
cross_layer_queries = 0
```

不再单独生成 cross-layer MQ。

### 4.3 Global MQ：24 个

令 28 个 local MQ 为 `L`，24 个 learned global queries 为 `Q_g`：

```text
Q = LN(Q_g)
K = V = LN(L)

A_g = CrossAttention(Q, K, V)
G_1 = Q_g + 0.1 * A_g
G   = G_1 + FFN_4x(LN(G_1))
```

其中：

```text
global query 数：24
hidden dim：1024
attention heads：8
attention residual scale：0.1
FFN ratio：4
```

learned query residual 用来保留 24 个 global slot 的身份，避免所有 global token 在 cross-attention 后完全收敛到同一表示。

### 4.4 Robot state：2 个 token

robot state 有两条注入路径：

```text
1. flatten(state history) → proprio MLP → state latent
   state latent 广播加到已有 52 个 MQ token 上

2. flatten(state history) → state token projection
   生成 2 个独立 state tokens，拼接到条件末尾
```

最终：

```text
C = [28 local, 24 global, 2 state]
C shape = [B, 54, 1024]
```

## 5. Observation latent

全部 54 个 condition tokens 经过共享 `fusion_norm` 后，用 gated weighted pooling 产生 `z_obs`：

```text
u_i = sigmoid(PoolGate(C_i))
s_i = PoolScore(C_i) + log(u_i)
w_i = softmax_i(s_i)

z_obs = sum_i w_i C_i
z_obs shape = [B,1024]
```

因此 `z_obs` 同时读取 local、global 和 state tokens，没有使用 obs8 或噪声起点消融。

## 6. Action Autoencoder

### 6.1 ActionEncoder

```text
输入：[B,8,7]
flatten：[B,56]
Linear：56 → 1024
6 × residual MLP：
    Linear 1024 → 1024
    GELU(tanh)
    Dropout(0)
    Linear 1024 → 1024
LayerNorm
Linear：1024 → 1024
输出 z_act：[B,1024]
```

### 6.2 ActionDecoder

```text
输入 z：[B,1024]
Linear：1024 → 1024
6 × residual MLP，结构同上
LayerNorm
Linear：1024 → 56
reshape：[B,8,7]
```

## 7. Latent Flow

### 7.1 Flow matching

训练时随机采样：

```text
tau ~ Uniform(0,1)
z_tau = (1-tau) z_obs + tau z_act
target_velocity = z_act - z_obs
```

Flow 预测：

```text
v_theta(z_tau, tau, C)
```

主 Flow loss：

```text
L_flow = MSE(v_theta, z_act - z_obs)
```

### 7.2 Flow 主干

```text
latent dim：1024
hidden dim：1024
Flow blocks：8
MLP ratio：4
attention heads：8
dropout：0
```

每个 Flow block 都读取相同的完整 `C=[local, global, state]`，但 8 个 block 的 cross-attention、token MLP、gate network 和 global relative gate 参数彼此独立。

每层结构：

```text
x_l
 ↓ LayerNorm
Q = Wq x_l

C
 ↓ LayerNorm
K = Wk C
V = Wv C

V' = gate_l(C, x_l, tau, state) * V
delta_l = Attention(Q, K, V')
x'_l = x_l + 0.1 * Wo(delta_l)

time embedding(tau)
 ↓ AdaLN parameters: gate, scale, shift
x_{l+1} = x'_l + gate * MLP(LN(x'_l)*(1+scale)+shift)
```

cross-attention 只门控 Value，不修改 Key 或 attention logits。

## 8. 当前门控设计

### 8.1 公共门控上下文

每个 Flow block 独立计算：

```text
h_i = TokenMLP_l([LN(C_i), source_embedding_i])
h_pool = Mean_i(h_i)
state_emb = Mean(state condition tokens)

z_l = GlobalMLP_l([
    h_pool,
    timestep_embedding,
    state_emb,
    current Flow query x_l
])
```

source embedding 将 token 区分为：

```text
H0 visual / H0 text / H1 / H4 / H8 / H12 / global / state
```

### 8.2 Local gate

28 个 local token 使用：

```text
a_i = GateMLP_l([h_i, z_l])
r_i = tanh(a_i)
g_i = 1 + 0.4 * (r_i - Mean_local(r))
```

所以 local gate 的组内均值固定为 1。

### 8.3 Global relative gate

24 个 global token 不再经过原 GateMLP，而使用相对 query-key score：

```text
h_i^rel = LN(LN(h_i) - Mean_global(LN(h)))
k_i = Wk_rel_l h_i^rel              # 1024 → 128
q_l = Wq_rel_l LN(z_l)              # 1024 → 128

a_i = (q_l · k_i) / sqrt(128)
a_i = a_i - Mean_global(a)
r_i = tanh(a_i / 1.0)
r_i = r_i - Mean_global(r)

g_i = 1 + 0.4 * r_i
```

关键设置：

```text
relative score dim：128
temperature：1.0
gate scale：0.4
Wq_rel：zero-init
每个 Flow block 独立一套参数
```

初始化时 global gate 为 1，随后根据当前 Flow latent、时间步、状态和 global token 相对差异学习分化。

### 8.4 State gate

两个 state token 的 gate 固定为 1，不参与 local/global 组内竞争。

## 9. 视觉动作效果辅助分支

该分支仅在训练时使用，不参与推理。

```text
当前主视角 I_t 与未来主视角 I_t+8
        ↓ 一次并行 Qwen vision forward
当前/未来视觉 token
        ↓ 各自通过同一个 Q4 learned-query resampler
V_t, V_t+8：[B,4,1024]

target = stopgrad(V_t+8 - V_t)

真实动作 z_act + V_t
        ↓ VisualDeltaDecoder
predicted visual delta：[B,4,1024]
```

损失：

```text
L_visual = SmoothL1(predicted_delta, target_delta)
weight = 0.01
```

本版本：

```text
ActionEncoder 不读取视觉 token
视觉分支只监督真实 z_act
Flow 预测出的 latent 不承担视觉重建
没有 ranking loss
没有 negative queue
没有多时距预测
没有 DINO 或 Qwen spatial 64-token teacher
```

注意：视觉 target 被 stop-gradient，但 Qwen VLM 本身仍参与策略联合微调；当前视觉 token 作为 decoder 条件的路径也没有整体冻结。

## 10. 完整训练目标

当前有效总损失：

```text
L =
    1.0  * L_flow
  + 0.2  * L_action_AE_recon
  + 1.0  * L_latent_consistency
  + 0.2  * L_flow_action_recon
  + 0.01 * L_visual_effect
```

其中：

```text
L_action_AE_recon：
    L1(ActionDecoder(z_act), action)

L_latent_consistency：
    MSE(z_pred_after_6_steps, z_act)

L_flow_action_recon：
    L1(ActionDecoder(z_pred_after_6_steps), action)
```

明确关闭：

```text
DCT loss = 0
MQ diversity loss weight = 0
MQ overlap loss = 0
MQ balance loss = 0
visual Flow loss = 0
action-effect rank loss = 0
progress loss = 0
goal-distance loss = 0
future image generation loss = 0
```

`MQ diversity` 仍会计算并记录，但权重为 0，不参与反向目标。

## 11. 训练与推理配置

### 11.1 训练

```text
总 batch size：16
per-device batch size：16
gradient accumulation：1
训练步数：160,000 optimizer steps
保存间隔：32,000
shuffle buffer：1,000
num_workers：2
seed：2026
```

优化器：

```text
AdamW
action-side LR：1e-4
VLM LR：1e-5
weight decay：0.01
warmup：8,000 steps
LR scheduler：cosine
gradient clip：1.0
```

当前训练参数量：

```text
action-side trainable：280,857,890
VLM trainable：852,985,920
合计 trainable：1,133,843,810
```

### 11.2 推理

```text
Flow Euler steps：6
exec horizon：8
每次预测 action chunk：[8,7]
```

视觉动作效果分支在推理中不执行。

## 12. 静态条件缓存状态

代码和磁盘配置现已支持：

```text
vita_flow_static_condition_cache: true
```

它只缓存同一 batch 内 7 次 Flow 调用共享的：

```text
condition projection
每个 block 的 token features
source embedding
relative global keys
cross-attention K/V
```

仍逐次计算：

```text
Q
Flow latent
timestep embedding
global context
gate scores
attention output
```

前向和梯度等价测试已经通过。

但是当前 PID 137406 在该缓存代码完成前已经启动，所以这个正在运行的 baseline 实际没有加载新缓存。若用当前磁盘配置重启，数学模型不变，但运行路径会启用缓存。

## 13. 当前实验依据

本 baseline 尚未到 32k，暂时没有闭环 eval 结果。

它的直接前代为：

```text
actioneffect_v3_compgate04_globalres01ffn4_finesrc...
```

前代结果：

```text
checkpoint 128k：LIBERO-Long SR 86.40
checkpoint 160k：LIBERO-Long SR 93.40
```

原始 ActionEffect-v3 Q4 baseline：

```text
checkpoint 128k：SR 88.00
checkpoint 160k：SR 94.60
```

因此，global residual + competitive gate 前代在单次评估中尚未超过原始 v3；当前 relative QK global gate 是否有效，必须等待相同节点的闭环 eval，不能根据训练 loss 判断。

## 14. 请重点审查的潜在问题

以下内容供下一轮方案评审，不代表已经确认的缺陷。

### 14.1 Global token 的差异是否具有语义

learned query residual 能保证 24 个 global token 数值不同，但不同可能主要来自固定 slot identity，而非读取了不同任务信息。需要联合检查：

```text
global attention map 差异
global token residual 前后 std
global gate std
遮挡/置换某个 global token 后的行为变化
```

### 14.2 Global gate 可能只学习 slot 偏好

relative gate 的 key 来自 global token，query 来自当前 Flow context。需要确认 gate 是否随：

```text
任务
观测
Flow block
Flow timestep
```

真实变化，而不是长期增强固定编号的 global slot。

### 14.3 z_obs 与 Flow condition 高度共享

`z_obs` 是对全部 54 个 C 的加权池化，Flow 每层又 cross-attend 同一批 C。起点和条件高度同源，可能产生冗余，也可能让 Flow 主要学习短残差。

### 14.4 Robot state 存在双路注入

state latent 被广播加到 52 个 MQ，同时又添加 2 个 state tokens；Flow gate context 还显式池化 state tokens。可能导致状态过度重复，且 state token gate 固定为 1。

### 14.5 8 个 Flow block 读取完全相同的条件池

各 block 参数独立，但 token 集相同。模型是否真正形成浅层/深层分工尚无直接监督或诊断。

### 14.6 固定 cross-attention scale 0.1

所有 block 的条件残差固定乘 0.1。它提高稳定性，但可能限制后期使用条件的能力；当前 token gate 只能重分配 Value，不能调整整个条件分支总强度。

### 14.7 视觉 teacher 并非真正冻结教师

future target 虽然 stop-gradient，但 Qwen 视觉编码器随策略训练更新；不同 step 的 target 表示会漂移。当前视觉条件分支也可向当前视觉特征回传梯度。

### 14.8 Q4 可能压缩过强

4 个无显式二维位置约束的 learned queries 需要表达视觉变化。它计算便宜，但可能丢失局部操作位置和小物体运动。

### 14.9 辅助视觉梯度可能干扰 ActionEncoder

`z_act` 同时服务动作重建和视觉变化预测。虽然视觉权重只有 0.01，仍应实测两项 loss 对 ActionEncoder 梯度的 cosine，而不能只比较 loss 数值。

### 14.10 训练成本较高

每个 batch 包含：

```text
1 次随机 tau Flow matching
6 次完整 Flow rollout
可训练的 12 层 Qwen VLM
当前/未来图像并行视觉编码
```

因此总耗时主要不只来自 gate。静态缓存只能消除重复条件投影，不能减少 8×7 次动态 Flow block 计算。

### 14.11 评价方差

当前主要依赖单次 500-episode LIBERO-Long SR。若两个方法相差约 1～2 个百分点，应补充相同 checkpoint、相同 episode seeds 或多 seed 评估，避免把随机波动当成结构收益。

## 15. 最小复现实验建议

为了判断当前新增 global relative gate 本身是否有效，最干净的比较是：

```text
A：原始 ActionEffect-v3 Q4
B：A + global query residual + FFN
C：B + local competitive gate + global relative QK gate（当前 baseline）
```

三者保持：

```text
同一 batch size
同一训练步数
同一 VLM 学习率
同一 visual loss
同一 6-step rollout
同一 eval seeds
```

优先比较 32k、96k、128k、160k 的任务级成功率，而不只比较总 SR。
